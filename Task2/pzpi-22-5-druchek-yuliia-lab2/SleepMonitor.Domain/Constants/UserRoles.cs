using SleepMonitor.Domain.Enums;

namespace SleepMonitor.Domain.Constants;

public static class UserRoles
{
    public const string Admin = nameof(UserTypeOptions.Admin);
    public const string User = nameof(UserTypeOptions.User);
}
